# Aetherium Ultra KEM — Dependencies
#
# Core: stdlib only (hashlib, hmac, os, struct).
# The KEM stub requires no external packages.

# -- Post-Quantum KEM backend (optional) -------------------------------------
# Uncomment ONE of the following to enable real ML-KEM (Kyber-1024).
# Without these, Aetherium falls back to the deterministic HMAC-SHA256 stub.

# pqcrypto>=0.1.3             # C-based Kyber-1024 (NIST Level 5)

# -- Development & testing ----------------------------------------------------
pytest>=7.4.0
